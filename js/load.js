const font_Logo = new FontFace('Logo', 'url(font/IBM-Logo.ttf)');
font_Logo.load().then((font) => {
  document.fonts.add(font);
});

const font_norm = new FontFace('Norm', 'url(font/ka1.ttf)');
font_norm.load().then((font) => {
  document.fonts.add(font);
});

const font_norm2 = new FontFace('Norm2', 'url(font/neonize.ttf)');
font_norm2.load().then((font) => {
  document.fonts.add(font);
});

const font_cmd = new FontFace('CMD', 'url(font/Caja.ttf)');
font_cmd.load().then((font) => {
  document.fonts.add(font);
});

const font_time = new FontFace('Time', 'url(font/nasalization.ttf)');
font_time.load().then((font) => {
  document.fonts.add(font);
});

const font_btc = new FontFace('BTC', 'url(font/ESTROG.ttf)');
font_btc.load().then((font) => {
  document.fonts.add(font);
});

